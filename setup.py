from setuptools import setup

setup(name = 'quantpy',
      version = '0.1.0',
      description = 'Package for general quant analysis',
      author='Stephon Henry-Rerrie',
      author_email='srerrie@gmail.com',
      packages = ["quantpy"]
)